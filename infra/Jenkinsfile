pipeline {
  agent any

  stages {
    stage('Init') {
      steps {
        sh 'cd infra/terraform && terraform plan -var-file="terraform.tfvars"'
      }
    }
    stage('Plan') {
      steps {
        sh 'cd infra/terraform && terraform plan'
      }
    }
  }
}
