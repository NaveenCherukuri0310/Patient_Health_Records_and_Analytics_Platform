pipeline {
  agent any
  stages {
    stage('Checkout') { steps { checkout scm } }
    stage('Backend Unit Tests') {
      steps {
        dir('backend') {
          sh 'pip install -r requirements.txt'
          sh 'pytest -q'
        }
      }
    }
  }
}
